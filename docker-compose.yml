services:
  agrimarket-db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "${AGRI_POSTGRES_PORT:-5433}:5432"
    environment:
      - POSTGRES_DB=${AGRI_POSTGRES_DB:-agrimarket}
      - POSTGRES_USER=${AGRI_POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${AGRI_POSTGRES_PASSWORD:-postgres}
    volumes:
      - agrimarket-pgdata:/var/lib/postgresql/data
    networks:
      - agrimarket-net

  agrimarket-api:
    build:
      context: .
      dockerfile: src/Bootstrapper/AgriMarket.Api/Dockerfile
    restart: unless-stopped
    ports:
      - "${AGRI_API_PORT:-5057}:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - ASPNETCORE_FORWARDEDHEADERS_ENABLED=true
      - ConnectionStrings__DefaultConnection=Host=agrimarket;Port=5432;Database=${AGRI_POSTGRES_DB:-agrimarket};Username=${AGRI_POSTGRES_USER:-postgres};Password=${AGRI_POSTGRES_PASSWORD:-postgres}
      - Jwt__Key=${AGRI_JWT_KEY:-dev-insecure-change-me-please}
      - Jwt__Issuer=${AGRI_JWT_ISSUER:-AgriMarket}
      - Jwt__Audience=${AGRI_JWT_AUDIENCE:-AgriMarket}
      - Cors__Origins__0=${AGRI_CORS_ORIGIN:-http://localhost:8080}
    depends_on:
      - agrimarket
    networks:
      - agrimarket-net

  agrimarket-web:
    build:
      context: .
      dockerfile: AgriMarket.Web/Dockerfile
    restart: unless-stopped
    ports:
      - "${AGRI_WEB_PORT:-5056}:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - ASPNETCORE_FORWARDEDHEADERS_ENABLED=true
      - ConnectionStrings__DefaultConnection=Host=agrimarket;Port=5432;Database=${AGRI_POSTGRES_DB:-agrimarket};Username=${AGRI_POSTGRES_USER:-postgres};Password=${AGRI_POSTGRES_PASSWORD:-postgres}
    depends_on:
      - agrimarket
    networks:
      - agrimarket-net

volumes:
  agrimarket-pgdata:

networks:
  agrimarket-net:
