stages:
  - deploy

deploy:
  stage: deploy
  only:
    - main
  tags:
    - csharp-backend
  script:
    - docker network create agrimarket-net 2>/dev/null || true
    - docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build --remove-orphans --detach
